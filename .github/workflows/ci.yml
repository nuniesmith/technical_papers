name: Build LaTeX Documents

on:
    push:
        branches:
            - main
        paths:
            - "**/*.tex"
            - ".github/workflows/ci.yml"
    pull_request:
        branches:
            - main
    workflow_dispatch:

jobs:
    build-latex:
        name: 📄 Build LaTeX PDFs
        runs-on: ubuntu-latest
        permissions:
            contents: write

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Install LaTeX dependencies
              run: |
                  sudo apt-get update -qq
                  sudo apt-get install -y -qq \
                    texlive-latex-base \
                    texlive-latex-extra \
                    texlive-fonts-extra \
                    texlive-science \
                    texlive-latex-recommended

            - name: Find and compile all main LaTeX documents
              run: |
                  echo "🔍 Finding LaTeX documents with \documentclass..."

                  # Find all .tex files containing \documentclass (main documents)
                  find . -name "*.tex" -type f ! -path "*/\.*" -exec grep -l "\\documentclass" {} \; > main_docs.txt

                  if [ ! -s main_docs.txt ]; then
                    echo "❌ No main LaTeX documents found"
                    exit 0
                  fi

                  echo "📄 Found $(wc -l < main_docs.txt) main document(s)"
                  cat main_docs.txt
                  echo ""

                  SUCCESS=0
                  FAIL=0

                  # Compile each document
                  while IFS= read -r tex_file; do
                    [ -z "$tex_file" ] && continue

                    dir=$(dirname "$tex_file")
                    base=$(basename "$tex_file" .tex)

                    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                    echo "📝 Building: $tex_file"
                    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

                    cd "$dir" || continue

                    # Compile twice for references/TOC
                    if pdflatex -interaction=nonstopmode -halt-on-error "$base.tex" && \
                       pdflatex -interaction=nonstopmode -halt-on-error "$base.tex"; then

                      if [ -f "$base.pdf" ]; then
                        size=$(du -h "$base.pdf" | cut -f1)
                        echo "✅ Success: $base.pdf ($size)"
                        SUCCESS=$((SUCCESS + 1))
                      else
                        echo "❌ Failed: PDF not generated"
                        FAIL=$((FAIL + 1))
                      fi
                    else
                      echo "❌ Failed: Compilation error"
                      FAIL=$((FAIL + 1))
                    fi

                    # Clean auxiliary files
                    rm -f *.aux *.log *.out *.toc *.synctex.gz *.fdb_latexmk *.fls 2>/dev/null

                    cd - > /dev/null
                    echo ""
                  done < main_docs.txt

                  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                  echo "📊 Build Summary: ✅ $SUCCESS succeeded, ❌ $FAIL failed"
                  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

            - name: List generated PDFs
              run: |
                  echo "📦 Generated PDFs:"
                  find . -name "*.pdf" -type f ! -path "*/\.*" -exec ls -lh {} \;

            - name: Upload PDF artifacts
              uses: actions/upload-artifact@v4
              with:
                  name: latex-pdfs
                  path: "**/*.pdf"
                  retention-days: 90

            - name: Commit PDFs to repository
              if: github.event_name == 'push' && github.ref == 'refs/heads/main'
              run: |
                  git config --local user.email "github-actions[bot]@users.noreply.github.com"
                  git config --local user.name "GitHub Actions"

                  # Add all PDFs
                  find . -name "*.pdf" -type f ! -path "*/\.*" -exec git add -f {} \;

                  # Commit if there are changes
                  if git diff --staged --quiet; then
                    echo "ℹ️  No PDF changes to commit"
                  else
                    git commit -m "🤖 Auto-compile LaTeX PDFs [skip ci]"
                    git push || echo "⚠️  Push failed (possibly no changes)"
                  fi

            - name: Summary
              if: always()
              run: |
                  echo "# 📄 LaTeX Build Complete" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY
                  echo "**Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
                  echo "**Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY
                  echo "Download compiled PDFs from the artifacts section above ⬆️" >> $GITHUB_STEP_SUMMARY
